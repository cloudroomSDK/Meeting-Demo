<Window x:Class="Meeting_WPF.InputWnd"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="InputWnd" Height="100" Width="495.522" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="99*"/>
            <ColumnDefinition Width="23*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>

        </Grid.RowDefinitions>
        <TextBox Grid.Row="0" Name="inputText" HorizontalAlignment="Stretch"  TextWrapping="Wrap" Text="" VerticalAlignment="Stretch" Grid.ColumnSpan="2" />
        <Button Grid.Row="1" Style="{StaticResource HighlightBtn}" Content="确认" HorizontalAlignment="Right" Margin="0,2,10,3" VerticalAlignment="Center" Height="30" Width="75" Grid.Column="1" Click="btn_ok_clicked"/>

    </Grid>
</Window>
